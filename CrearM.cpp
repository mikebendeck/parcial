#include <iostream>
#include <vector>

std::vector<int> SeleccionOrdenamiento(const std::vector<int> arr) {
    std::vector<int> vectorOrdenado;

    for (int i = 0; i < arr.size(); i++) {

        int minPos = i;

        for(int j = 0; i < arr.size() +1, j++){}

    }

    return vectorOrdenado;
}